<div class="container">
  <div class="tagged-header my-4">
    <h2>Bands tagged with <%= params[:tag] %></h2>
  </div>
  <div class="tagged-bands">
    <% @bands.each do |band| %>
      <%= render 'bands/card', band: band %>
    <% end %>
  </div>
</div>
