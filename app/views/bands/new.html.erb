<h1> Create a Band </h1>
<%= render 'form' %>

<div class="container" style="background-color: white; width: 500px; margin-top: 100px; padding: 30px">
  <% if user_signed_in? %>
    <h2>Create a Band</h2>
    <%= render 'form' %>
  <% end %>
</div>
