<%= simple_form_for @band do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price, label: 'Price per hour' %>
  <% if @band.id.nil? %>
    <%= f.submit 'Create' %>
  <% else %>
    <%= f.submit 'Edit' %>
  <% end %>
<% end %>
