<% if user_signed_in? %>
  <div class="container">
    <div class="tagged-header my-4">
      <h2>My bands</h2>
    </div>

    <div class="row" id="my-bands">
      <% @bands.each do |band| %>
        <%= render 'bands/card', band: band %>
      <% end %>
    </div>
  </div>
<% end %>
